{% set name = "rocksdict" %}
{% set version = "0.3.26" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/Congyuwang/RocksDict/archive/v{{ version }}.tar.gz
  sha256: 3ab48aa011e9e1600ccad4fced019278ea2a8373147e8c7e3455e86916771e03

build:
  skip: true  # [py<37]
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0
  script_env:
    - LIBCLANG_PATH=$BUILD_PREFIX/lib  # [not win]

requirements:
  build:
    - {{ stdlib('c') }}
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('rust') }}
    - clangdev
  host:
    - python
    - maturin >=1.0,<2.0
    - pip
  run:
    - python

test:
  imports:
    - rocksdict
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/Congyuwang/RocksDict
  doc_url: https://congyuwang.github.io/RocksDict/rocksdict.html
  summary: Python fast on-disk dictionary; RocksDB & SpeeDB Python binding
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - thewchan
